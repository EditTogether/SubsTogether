name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x, 18.x]

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'yarn'
        cache-dependency-path: 'www/client/yarn.lock'
    - name: Install dependencies
      run: cd www/client && yarn install
    - name: Build project
      run: cd www/client && NODE_OPTIONS="--openssl-legacy-provider" yarn build
    - name: Run tests
      run: cd www/client && yarn test --passWithNoTests
